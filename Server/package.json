{
  "name": "fastmail-push",
  "version": "1.0.0",
  "description": "New-mail pushes for the Fastmail shell apps on iOS",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test"
  }
}
